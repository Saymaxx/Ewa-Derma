{
  "name": "ewa-derma-clinic-system",
  "version": "1.0.0",
  "private": true,
  "description": "Ewa Derma Clinic Management System - Monorepo",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,yellow\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
